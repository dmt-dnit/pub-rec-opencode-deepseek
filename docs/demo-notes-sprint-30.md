# Demo Notes Sprint 30

- Good reviewer-story sprint: Codex caught a real UX/auth bug in Sprint 28, and Sprint 30 fixed it without widening backend trust.
- Useful talking point: UI guards are for navigation polish here, not the security boundary. The actual boundary stayed on the backend.
- Demo proof to show later: hard-refresh `/admin` as a real admin now stays on the page instead of bouncing to `/dashboard`.
