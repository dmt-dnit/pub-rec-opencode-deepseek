# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

A demo/learning monorepo of independent services that together illustrate JWT-based SSO + a choreographed saga on Kafka:

- `shared-model` — Maven library of cross-service **contracts** (DTOs, Kafka events). No service-specific code.
- `auth-server` — Spring Boot SSO/auth service (port **9000**). Issues RSA-signed JWTs, exposes JWKS, email/password + Google OAuth2 login.
- `order-service` (port **8080**) and `inventory-service` (port **8081**) — two Spring Boot services that implement a **choreographed saga**: Order Service publishes `OrderPlacedEvent`, Inventory Service reserves stock and responds with `InventoryReservationEvent`, Order Service consumes the response and updates order status. No central orchestrator.
- `order-ui` (port **4200**) and `inventory-ui` (port **4201**) — two Angular 22 SPAs, each paired 1:1 with its corresponding backend service.

See `docs/adr/0001-event-driven-showcase-architecture.md` for the canonical architecture description.

There is no root build — each subdirectory is built/run independently. Build artifacts are **git-ignored** (`.gitignore` covers `**/target/`, `**/dist/`, `**/node_modules/`, `**/.angular/`, etc.) — they are NOT committed. **Consequence for reviewers/CI: never treat a `target/` jar or `surefire-reports/*.xml` in a working copy as evidence — it may be stale local build output. Always `mvnw clean verify` (or read the live CI run, which builds from source) before judging behavior.** (History: artifacts *were* committed pre-2026-06-19; the `.gitignore` added then stopped that, but stale local `target/` dirs can linger in a checkout — `git clean -xfd` clears them.)

## Multi-agent development workflow

This is the showcase project for Dimitri and his son's IT startup, built with a multi-agent loop rather than a single developer:

- **opencode + DeepSeek** — implementers. Each picks up one task brief at a time and turns it into code changes.
- **Claude Code (this assistant)** — sprint coordinator. Writes per-task briefs, verifies completed work against acceptance criteria by reading the actual code/diffs (never by trusting the implementing agent's self-report), and writes a handoff doc for Codex.
- **Codex** — reviewer. Reads the handoff, re-verifies independently, and issues an accept/reject verdict with blockers. That verdict gates whether the sprint closes and the next one starts.

### File conventions
- `docs/backlog/sprint-N.md` — sprint overview: why it exists, task list, recommended order.
- `docs/backlog/tasks/sprint-N/<ID>-<slug>.md` — one brief per task (e.g. `F-1-fix-frontend-proxy-routing.md`), each with acceptance criteria.
- `docs/backlog/sprint-N-handoff.md` — point-in-time status check written by Claude before sending the sprint to Codex, verified against acceptance criteria by reading code. **Must carry a `Review-Target-Commit: <sha>` line** — the fix commit Codex is being asked to review — so `scripts/verify-review.sh` can mechanically confirm the returned verdict actually inspected that commit.
- `reviews/sprint-N-track-X-review.md` — Codex's review verdict. If rejected, its blockers become the next sprint's backlog (e.g. `sprint-2.md`'s "Why this sprint exists" cites `reviews/sprint-1-track-a-review.md` directly). Codex must open each review with the machine-readable header from `docs/backlog/review-machine-header.md` (`Review-Target-Commit:` + `Verdict: ACCEPT|REJECT`) so freshness/verdict are parseable; `scripts/verify-review.sh` tolerates the legacy freeform `Review target:` line for older files.
- `docs/demo-notes-sprint-N.md` — Codex's short presentation-prep note after each review: demo-worthy story beats, screenshots/video captured or missing, commands worth replaying, and any evidence the conference-prep session should ingest.

### Sprint cadence
1. Claude writes/refines task briefs for the sprint. Before writing a new sprint's briefs, check whether any major framework/runtime dependency (Angular, Spring Boot, the pinned Java version, Kafka client libs) is approaching or past its LTS/support window — don't wait for `npm audit` or a security review to surface it. See "Dependency currency" below for why this matters.
2. opencode+DeepSeek implement each brief — prefer isolated worktrees/branches per task. Sprint 1 ran concurrent edits on one shared tree, which produced incomplete renames across files; don't repeat that.
3. Claude verifies against acceptance criteria by reading the actual diffs/code, then writes a handoff doc.
3.5. Before telling Codex the sprint is ready: run `scripts/pre-review-check.sh <sprint-number>`. It fails closed if the working tree isn't committed or the handoff doc is missing — exactly the gap that let Sprint 4 round 2 reach Codex with 2 of 5 tasks unstarted and no commit checkpoint. Don't skip this because "DeepSeek says it's done" — that self-report is the thing this whole cadence exists to not trust.
4. Codex reviews independently and gives a verdict.
4.5. When the verdict lands, run `bash scripts/verify-review.sh <sprint-number>` before acting on it — this is the mechanical stale-review gate that closes the loop (added Sprint 20, AUTO-2; born from the Sprint 19 stale false-reject where a "resumed" Codex review re-stated round-1 findings against pre-fix code). It picks the highest-round review file, parses the reviewed commit + verdict, and returns: **exit 0 = FRESH+ACCEPT** (verdict trustworthy → close the sprint), **exit 2 = STALE** (the review did not inspect `Review-Target-Commit` → do NOT trust the verdict; re-request a fresh review — this is the AUTO-1 dedup symptom), **exit 3 = FRESH+REJECT** (genuine blocker → next sprint's backlog), **exit 4 = UNKNOWN** (missing `Review-Target-Commit` in the handoff, unparseable verdict, or unknown commit → fix the metadata, don't guess). Only exit 0 may close a sprint.
5. Reject → blockers become the next sprint's backlog. Accept → next sprint/track starts.

### Merge gate: direct-to-main vs. review-branch (decided 2026-07-14, Sprint 21; scope narrowed same day)

Scope is the **supervision boundary**, not the mechanical/logic-bearing split:

- **Coordinator-supervised sprint work** — everything produced through the normal sprint cadence (opencode/Claude-agent implements, coordinator reads every diff, Dimitri is in the loop at sprint boundaries), mechanical or logic-bearing alike. Commit **and push** directly to `main`, same as every sprint to date. Codex review stays post-hoc. Across 21 sprints this has been safe and cheap to recover from — the two times a reject left bad code on `main` (Sprint 16 round-1, Sprint 19 round-1) it was caught and fixed forward the same cycle, because a human was watching every push. `main` has no other live consumer today (no auto-deploy, no one else branching off it) for a stability gate to actually protect.
- **AUTO-3 operational-driver pushes (once built)** — the one case where a push happens genuinely unattended, on a schedule, with no one watching in real time. This tier pushes to a dedicated review branch first; Codex reviews that branch/commit (needs a real PR, since CI's `pull_request` trigger is what gives Codex a live-CI signal off `main`); `main` is fast-forwarded/merged only after a genuine ACCEPT (`scripts/verify-review.sh` exit 0). Do not let the driver push straight to `main`.

Why this scope, not a blanket logic-bearing rule: the original motivation (keep `main` stable like at work) is about protecting concurrent builders/deployers from a broken shared branch — neither exists here yet, and coordinator-supervised work already has an in-cycle recovery track record. A branch-gate on *every* logic-bearing task would add real ceremony (opening real PRs to get a CI signal, more moving parts in the AUTO-1/2/3 review-intake tooling) to work that's already supervised, without addressing the actual new risk. That risk is specifically the AUTO-3 driver pushing with nobody watching — see `[[project-automation-roadmap]]`: the human-approval gate was always meant to live at a future prod boundary; the driver's unattended pushes are the first thing in this repo that actually needs one.

### Who implements what

Default: opencode+DeepSeek implement all code changes via isolated worktrees. Claude is coordinator, not implementer.

**Coordinator-direct** is acceptable only for changes where the diff is trivially obvious, non-controversial, and fully verifiable by reading alone: updating a snapshot paragraph in CLAUDE.md, fixing a doc typo, removing a clearly dead import. Not for logic, type, or config changes — even one-liners. Sprint 11 O-1/O-2 went coordinator-direct and passed Codex, but the precedent is bad: it skips the agent self-report / coordinator diff-check / Codex code-review chain that the whole workflow depends on.

**Claude agent (worktree) as fallback** — if opencode is unavailable or repeatedly failing, spawn a Claude subagent with `isolation: "worktree"` as a stand-in implementer. Use `model: "haiku"` for mechanical tasks (lockfile edits, selector fixes, single-import tweaks) and `model: "sonnet"` or `model: "opus"` for logic-heavy work. The same rules still apply: worktree isolation, coordinator reads the actual diff to verify, handoff doc written before Codex sees it. A Claude agent is not exempt from the "show actual output, not asserted Pass" standard.

### Dependency currency
Angular 18.2.x (this repo's original pin) was very likely the correct LTS choice when the project was set up — Angular's LTS window is 18 months from release, and 18 was current/active at that time. The problem that produced sprints F-6/G-4/H-3 wasn't the initial pick, it was two sprints' worth of silent drift afterward: nobody checked dependency currency until `npm audit` forced the question, by which point the project was several majors behind and facing one large, risky `ng update` migration instead of several small, cheap ones along the way. Checking EOL/LTS status at the start of each sprint (step 1 above) turns that into routine hygiene instead of a late, compounded surprise — this applies to the Java version pin and Spring Boot/Kafka client versions too, not just Angular.

### Verification standards for implementers
Sprints 1-3 showed a recurring failure mode: a task report claims a check "Pass" based on a proxy signal (a file matching a known-good template, an unrelated test going green, general knowledge of a package's patched version) rather than actually exercising the real check on the real target. Concrete examples Codex caught after a "Pass" was reported: `mvnw.cmd` matched the genuine Maven Wrapper template byte-for-byte but still errored in real PowerShell; a test suite passed while the logs showed the intended fix (a Mockito mock-maker override) wasn't actually the reason it passed; a specific patched-version number was reported without re-checking current advisory data.

To avoid a fourth round of this: when a task brief's acceptance criteria includes running a command or reproducing a result, **show the actual command output**, don't just assert "Pass/Fail." If the target platform/environment for a check isn't available to you (e.g. no Windows/PowerShell, no Docker/Podman daemon, no access to the specific JDK vendor a reviewer uses), **say so explicitly** rather than inferring success from whatever you can run instead. A stated limitation is useful information for the next reviewer; an unverified "Pass" that turns out false costs a whole sprint round.

### Sprint rules card (coordinator + reviewer reference)

This is the compact ruleset distilled from the Track A retrospective. Full context: `docs/backlog/track-a-retro.md`.

**Coordinator (Claude) — every sprint:**
1. Acceptance criteria = observable outcome, not implementation step. Rewrite if it can only be verified by reading intent.
2. Read the actual template/DOM before writing any E2E selector. Include the relevant snippet in the brief.
3. E2E tasks assume dirty DB. Idempotency across repeated runs is a first-class criterion.
4. Max ~3 loosely coupled tasks per sprint. Tightly coupled tasks go in one brief. Break large tasks before briefing.
5. Note agent choice in every handoff. Default: haiku for mechanical, sonnet/opus for logic. OpenCode+DeepSeek only on explicit trigger (two failed rounds, ambiguous architecture, security/performance, genuine second-opinion value).
6. Worktree agent: verify the branch has new commits. If not, apply diff manually to main and note it in handoff.
7. `bash scripts/pre-review-check.sh <N>` must pass before handing to Codex. No exceptions.
8. Handoff must include: exact commits · done/not-done · actual command output · what couldn't run and why · browser status ("passed" or "Codex-only") · `git status --short` confirming clean tree.
9. `npm audit --omit=dev` is the pass/fail signal. Full audit goes in caveats, not the scorecard.
10. Update CLAUDE.md status snapshot every sprint.
11. When a Codex verdict lands, run `bash scripts/verify-review.sh <N>` and act on the exit code (0 = close · 2 = STALE, re-request · 3 = genuine reject → backlog · 4 = fix metadata). A STALE or UNKNOWN result **never** closes a sprint. The handoff must carry `Review-Target-Commit:` for this to resolve without passing the sha explicitly.
12. Coordinator-supervised sprint work (mechanical or logic-bearing) pushes straight to `main`, same as always (see "Merge gate" above). Only the AUTO-3 operational driver's unattended pushes go through a review branch held until Codex ACCEPTs.

**Reviewer (Codex) — every review:**
1. Check acceptance criteria format — flag "use this locator" criteria, don't just test against them.
2. Browser tasks must say "browser accepted" or "Codex-only verification." No silent hand-offs.
3. E2E tasks: clean or dirty DB? Dirty is the default; clean-only is a weaker result.
4. `git status --short` must be clean. Generated artifacts in the diff are a flag.
5. Standing caveats: don't re-litigate. Check only if the signal changed. Reference the caveats table in this file.
6. Write the sprint review to `reviews/*` and also write `docs/demo-notes-sprint-N.md` for the conference-prep thread. If there is nothing presentation-worthy, say that explicitly in the note.
7. No source edits from the reviewer path.
8. Open every review with the machine header (`Review-Target-Commit:` + `Verdict: ACCEPT|REJECT`) per `docs/backlog/review-machine-header.md`, and write a **new** `reviews/sprint-N-…-round-K-review.md` file for each round — never "resume" and re-state a prior round in place (that produced the Sprint 19 stale false-reject, which `scripts/verify-review.sh` now catches mechanically).

### Current status snapshot (2026-07-15)

**Current track:** Track C — go-live (opened 2026-07-15, `docs/backlog/track-c-go-live-roadmap.md`). Track B — hardening (unblocked 2026-06-28) is functionally drained; see its history below.  
**Last approved sprint:** Sprint 31 (JVM heap caps) — Codex genuine **ACCEPT** 2026-07-17 (`reviews/sprint-31-track-c-review.md`, target `12038b3`, `scripts/verify-review.sh 31` → exit 0 FRESH+ACCEPT). Sprint 30 (admin guard false-rejection fix) also genuine ACCEPT same day (`reviews/sprint-30-track-c-review.md`, target `3f092b9`, exit 0). See below for full detail. (Prior: Sprint 16 (Track B Sprint 3) — approved by Codex on 2026-06-29 (round 3, `reviews/sprint-16-track-b-round-3-review.md`); live CI incl. Snyk SCA green, Swagger/`/v3/api-docs` 200 on all 3 services, authenticated API probe, Playwright smoke passed. Delivered **SB-3** (clean Boot 4.1.0 / modular starters / real Jackson 3 — Sprint 15 shims removed), **B-4** (springdoc 3.0.3 Swagger), **SEC-1** (Snyk SCA in CI — swapped from OWASP after its NVD download proved too slow — + Dependabot). 3 rounds: r1 reject = SB-3 silently reverted by a stale-base agent's whole-file poms (recovered); r2 reject = OWASP NVD-download hang (→ Snyk). Spring Security 7.1.0 (patched). (Sprint 15 — Boot 3.4.3→4.0.7 migration — approved 2026-06-29.))  
**Last cleared:** Sprint 31 (JVM heap caps) — cleared 2026-07-17, see below. Sprint 30 (admin guard false-rejection fix) — cleared 2026-07-17, see below. Sprint 29 (persistent auth-server DB) — cleared 2026-07-17, see below. Sprint 28 (admin role promotion + approval UI + role-scoped access) — Codex **REJECTED** (P1: admin guard false-rejection on hard refresh), fixed by Sprint 30 (same pattern as Sprint 26→27), see below. Sprint 27 (Google OAuth redirect fix) — cleared 2026-07-15, see below. Sprint 26 (Track C Phase 4, Vercel deploy prep) — Codex **REJECTED** (P1: OAuth redirect regression), fixed by Sprint 27, see below. Sprint 25 (`com.example`→`be.dnit` rename) — Codex review explicitly skipped per Dimitri's call, see below. Sprint 24 (OutboxRelay self-invocation fix) — cleared 2026-07-15, see below. Sprint 23 (Track C Phase 3.5, Kafka deploy artifacts) — cleared 2026-07-15, see below. Sprint 22 (Track C Phase 3, deploy artifacts for `dnit-vps`) — cleared 2026-07-15, see below. Sprint 21 (dependency-currency triage) — cleared 2026-07-14. Sprint 19 (Track B Sprint 6) — cleared 2026-07-03 (Codex round-2 genuine ACCEPT; B-8 fallback hardening + B-9 CI e2e-smoke). Sprint 18 (Track B Sprint 5) — B-6 compose healthchecks + F2 smoke, cleared at source 2026-07-01 (`reviews/sprint-18-track-b-review.md`, Codex endorsed dropping the risky ZK `ruok` healthcheck); **its runtime carryover is proven by Sprint 19's green `e2e-smoke` CI job.** (Sprint 17 cleared 2026-07-01.)
**Sprint 29 (persistent auth-server DB) — CLEARED 2026-07-17.** Codex genuine **ACCEPT** (`reviews/sprint-29-track-c-review.md`, target `cabc84c`, `scripts/verify-review.sh 29` → exit 0). The very first backend redeploy after Sprint 28 wiped Dimitri's account/admin role — `auth-server` ran H2 pure in-memory. Root cause was two independent things, found by reading the actual config: `jdbc:h2:mem:authdb` (in-memory by definition) *and* `ddl-auto: create-drop` (drops+recreates schema on every shutdown regardless of storage backend) — fixing only one would still have wiped everything. Deliberately smaller than the deferred Phase 8 (Postgres migration): kept the existing embedded H2 engine, switched only `ddl-auto` to `update` in `application.yml` (datasource URL stays `jdbc:h2:mem:authdb` there, correct for local dev/CI), and documented the production override (`SPRING_DATASOURCE_URL=jdbc:h2:file:/opt/pubrec/auth/data/authdb`) in `deploy/systemd/env-examples/auth.env.example` — Spring Boot's relaxed env-var binding needs no code change for the override itself. Coordinator ran a real two-process restart test (register a user, kill the JVM outright, restart, confirm the user survived) rather than trust a green build alone. **A genuine environment gotcha surfaced during this verification, now documented** (`[[feedback-wsl-java-interop-shim]]`): the bare `java` command in this WSL box resolves to a Windows-interop shim (confirmed via the startup log showing `Java 25` and a `C:\...` path) that silently ignores `JAVA_HOME` and doesn't forward environment variables — cost real debugging time before being traced; `./mvnw` was never affected. Codex independently re-ran the Java 21 Enforcer check from integrated `main` and confirmed the two persistence fixes were both present and sufficient; it noted (correctly, not a blocker) that it didn't reproduce the coordinator's live restart test or the VPS provisioning itself. **Live-apply completed same day**: Dimitri ran the one-time VPS provisioning (`mkdir`/`chown`/env-file edit — coordinator has no sudo for this, confirmed directly), then the coordinator proved persistence on the actual public `saga-auth.dnit.be` across **two consecutive live redeploys** (registered a test account, redeployed, confirmed survival; redeployed again, confirmed survival again) before approving and promoting Dimitri's real account to `ADMIN` — the last manual intervention this should ever need.
**Sprint 31 (JVM heap caps) — CLEARED 2026-07-17.** Codex genuine **ACCEPT** (`reviews/sprint-31-track-c-review.md`, target `12038b3`, exit 0 FRESH). Triggered by a real incident: `dnit-vps` went fully unresponsive (load average **156**, swap **100% full**) — trigger was a stuck, unrelated Pet-Giftshop LXD container extraction, but the box had zero headroom to absorb it because none of this project's 3 Spring services nor Kafka/ZooKeeper had any JVM heap cap, all running unconstrained default sizing on a ~961Mi box shared with several other tenants. Root cause of `inventory-service` specifically running at 275MB (vs `order-service`'s 69MB) verified, not guessed: `order-service` has zero `@Scheduled` tasks, `inventory-service`'s `OutboxRelay` (Sprint 24) polls every ~500ms continuously — that sustained churn grows an uncapped heap. Added `-Xms64m -Xmx160m` to auth/order, `-Xms64m -Xmx224m` to inventory (higher, justified above), `KAFKA_HEAP_OPTS` capping Kafka from its unconfigured 1GB default to 512MB and ZooKeeper to 256MB. **Live-applied same day, with Dimitri's temporary elevated grant** (`/opt/ops/grant-temp-sudo.sh claude-coordinator`, same time-boxed pattern as Sprint 22): all 3 services restarted one at a time (verifying health between each, per this project's own concurrent-restart-race history), Kafka/ZooKeeper containers recreated with the new env vars, both dependent services confirmed reconnecting cleanly (no stuck retry loops), and a **real order placed through the live public API closed the full saga loop** (`PENDING`→`CONFIRMED`) as the final proof. Grant revoked immediately after. Dimitri's explicit sequencing: optimize footprint first, only consider a bigger VPS tier if incidents recur with caps in place.
**Sprint 30 (admin guard false-rejection fix) — CLEARED 2026-07-17.** Codex genuine **ACCEPT** (`reviews/sprint-30-track-c-review.md`, target `3f092b9`, exit 0 FRESH). Fixed the exact Sprint 28 P1 blocker: `order-ui`'s `admin.guard.ts` required `currentUser.role === 'ADMIN'` synchronously, but `AuthService` only populates that asynchronously via `fetchMe()` — on a hard refresh directly on `/admin` (an entirely ordinary way to reach a bookmarked page), a legitimate admin got bounced to `/dashboard` before the `/me` response arrived. **Coordinator scoping error, not an implementer miss**: the original Sprint 28 brief called this an "acceptable edge case" — on reflection (and per Codex) it's the default outcome for any direct nav/refresh, not a rare race. Fix aligns `admin.guard.ts` with `auth.guard.ts`'s already-correct permissive pattern (null role passes through, only an affirmatively-known-wrong role rejects) — one file, one line changed. Live-verified in the deployed bundle by fetching the actual chunk and confirming the exact minified pattern (`t&&t!=="ADMIN"?...:true`) is present.
**Sprint 28 (admin role promotion + approval UI + role-scoped access) — Codex REJECTED, round 1, 2026-07-17** (`reviews/sprint-28-track-c-review.md`, target `2feba3a`). Two real operational needs surfaced by actually using the live system: (1) Dimitri had to be approved manually via `curl` after registering — no UI existed for this; (2) **Dimitri caught live** that any authenticated account, regardless of role, could use *both* `order-ui`/`order-service` and `inventory-ui`/`inventory-service` — no role enforcement existed beyond "valid JWT." Three tasks, each opencode+DeepSeek in its own worktree: **A-1** (`8499483`, auth-server) new `PUT /api/admin/users/{id}/role` (validates against the real `Role` enum, 400 on garbage) + `@JsonIgnore` on `UserEntity.password` fixing a real, live bcrypt-hash leak in `GET /api/admin/users` found while scoping the UI that consumes it; **A-2** (`4dbc6f9`, order-ui) new `/admin` page (list + approve, admin-only guard) — **deliberately not built for inventory-ui too**, an open question revisited below; **A-3** (`2feba3a`) restricts `order-service`/`order-ui` to `CUSTOMER`+`ADMIN` and `inventory-service`/`inventory-ui` to `WAREHOUSE_STAFF`+`ADMIN`, backend `hasAnyRole` as the real enforcement, frontend guards as UX only — zero test regressions, verified *before* scoping that the one relevant test (`OrderEventIntegrationTest`) disables Spring Security's filter chain entirely. **A real security decision, not a shortcut**: this repo is public on GitHub, so promoting Dimitri's own account was deliberately *not* done by seeding his real email/password into `DataSeeder.java` (would expose a login credential publicly) — the new role-promotion endpoint exists specifically so the coordinator could promote his *already-registered* account (his own password, never touching source) instead. **Fully live-verified after redeploy**: role-scoping confirmed both directions on live traffic (`customer1`→order 200/inventory 403, `warehouse1`→inventory 200/order 403), password-leak fix confirmed absent from the live response, bad-role validation confirmed 400. **Open, undecided as of 2026-07-16**: whether to add the same admin approval page to `inventory-ui` (cheap follow-up, not a functional gap since approvals are global via `auth-server` — order-ui's page already covers every account) — Dimitri leaned toward "yes" but it's not committed; a related gut-check on whether roles need to become more fine-grained (multi-role-per-user, or a permission separate from full `ADMIN`) was explicitly deferred, revisit only if this grows toward a prod-grade project, not before. **Codex's round-1 finding (P1, admin guard false-rejection on hard refresh) fixed in Sprint 30, which got its own genuine ACCEPT** — same reject→next-sprint-fixes-it pattern as Sprint 26→27.
**Sprint 27 (Google OAuth redirect fix) — CLEARED 2026-07-15.** Codex genuine **ACCEPT** (`reviews/sprint-27-track-c-review.md`, target `0846bc5`, `scripts/verify-review.sh 27` → exit 0). Fixed the exact Sprint 26 P1 blocker: both UIs' `loginWithGoogle()` now builds `` `${environment.authApiBase}/oauth2/authorization/google` `` instead of a bare relative path (`order-ui`/`inventory-ui` `src/app/pages/login/login.component.ts`), so the button reaches `https://saga-auth.dnit.be` instead of silently reloading the Vercel-hosted SPA. Dev behavior unchanged (`authApiBase` still `''`). Codex confirmed the exact call site fixed and prod/dev environment values both correct; noted it couldn't reproduce the Angular build itself (Windows review session, same platform-mismatch class of issue the coordinator hit and resolved in Sprint 26) — coordinator independently rebuilt both UIs and grepped the actual deployed chunk files. **Fully live-verified end-to-end, not just merged:** both Vercel frontends redeployed via their deploy-hook workflows and the fix confirmed live by fetching the actual lazy-loaded login chunk from both `pub-rec-saga-orders-ui.vercel.app` and `pub-rec-saga-inventory-ui.vercel.app` (main.js doesn't contain the login code — it's a separate route-lazy chunk, found via main.js's chunk-reference list). Separately, all 3 backend services (`deploy-auth.yml`/`deploy-order.yml`/`deploy-inventory.yml`) redeployed with Dimitri's go-ahead to pick up Sprint 26's V-2 CORS change, which had been merged but not yet live — confirmed via direct CORS preflight probes against `saga-{auth,orders,inventory}.dnit.be` that each service now correctly echoes back `Access-Control-Allow-Origin` for its expected Vercel origin (previously: the Vercel origin got `403 Invalid CORS request` while the old `localhost:4200` origin still got `200`, proving the live jars were stale relative to `main`). **Track C Phase 4 is now fully done: code reviewed and ACCEPTed, both frontends live on Vercel, both backend CORS + OAuth-redirect fixes live-verified working end-to-end on the public split-origin topology.**
**Sprint 26 (Track C Phase 4, Vercel frontend deploy prep) — Codex REJECTED 2026-07-15, round 1** (`reviews/sprint-26-track-c-review.md`, target `9902156`). Dimitri created the two Vercel projects (`pub-rec-saga-orders-ui.vercel.app`, `pub-rec-saga-inventory-ui.vercel.app`) after a prior session crash; this sprint wired the code side across 3 tasks, each opencode+DeepSeek in its own worktree: **V-1** (`b7324de`) env-based API base URLs (`src/environments/environment{,.prod}.ts`) + `angular.json` `fileReplacements`/`defaultConfiguration: production` + `vercel.json` SPA fallback for both UIs; **V-2** (`b3ddf7b`) CORS allow-list extended on all 3 Spring services for the two Vercel origins (`auth-server` gets both, each backend service gets only its own UI's origin); **V-3** (`9902156`) two new `workflow_dispatch`-only GitHub Actions deploy-hook workflows, deliberately not push-triggered (follows this repo's existing backend deploy workflows, not the Pet Giftshop push-trigger reference the pattern was adapted from). Coordinator verified by a full independent rebuild on integrated `main` (all 4 Maven modules + both `ng build`s) and, critically, grepped the built `dist/browser/*.js` bundles for the literal prod origin strings to prove `fileReplacements` actually fired, not just that the build was green. **Confirmed and flagged for Dimitri: Vercel's Output Directory must be set to `dist/browser`, not `dist`** — this builder (Angular 22, no SSR) always nests browser output under `browser/`; Dimitri set this correctly during the live-apply. Hit a real multi-layer environment detour verifying V-1 (coordinator node_modules-copy race, a pre-existing stale partial install in the main repo's own `inventory-ui/node_modules`, and transient WSL `/mnt/c` npm rename flakiness) — none were code defects, full account in the handoff doc. **Codex round-1 REJECTED on one genuine finding (P1):** both UIs' Google login button (`loginWithGoogle()`) still used a frontend-relative `/oauth2/authorization/google` path — a **coordinator brief error**, not an implementer miss: V-1's brief enumerated the REST services and WebSocket service needing environment-based origins but never mentioned this login entrypoint, so it silently broke under the new split-origin topology (the Vercel host's own `vercel.json` SPA-fallback rewrite would catch that path instead of ever reaching `saga-auth.dnit.be`). Fixed in Sprint 27 (see above).
**Sprint 25 (`com.example`→`be.dnit` package/groupId rename) — Codex review explicitly SKIPPED 2026-07-15, per Dimitri's call.** Mechanical but wide (65 Java files moved via `git mv`, all 4 `pom.xml` groupIds, `application.yml` trusted-packages strings, `.github/workflows/ci.yml` comments) — see `docs/backlog/sprint-25-handoff.md` for the full account, including a false-alarm `git diff --stat` cross-rename display artifact (confirmed non-issue by reading both files' actual content) and a first dispatch attempt that hit a git `index.lock` collision and produced nothing (recovered, redispatched cleanly). Coordinator-verified via full independent rebuild from integrated `main`: all 4 modules `BUILD SUCCESS`, Kafka-heavy integration tests passing (proof the trusted-packages strings were updated correctly, since a wrong string fails at deserialization/test time not compile time), and a fresh `grep -rl "com\.example"` returning empty. **This sprint was originally scoped as "before Phase 4 resumes" and intended to get a full Codex review**, but `scripts/verify-review.sh 25` still returns **UNKNOWN** (no review file exists) — Dimitri made an explicit call to skip requesting that review and proceed straight to Sprint 26, given the rename's already-thorough coordinator verification. Logged here so this isn't mistaken for an oversight if it comes up later.
**Sprint 22 (Track C Phase 3, deploy artifacts) — CLEARED 2026-07-15.** Codex round-2 **genuine ACCEPT** (`reviews/sprint-22-track-c-round-2-review.md`, target `341554b`, machine header compliant this round). Delivered the deploy-artifact half of hosting `auth-server`/`order-service`/`inventory-service` on Dimitri's existing `dnit-vps`: **V-1** systemd units (`ddde900`), **V-2** Nginx vhosts for `saga-{auth,orders,inventory}.dnit.be` (`6bfba06`), **V-3** GitHub Actions deploy workflows + shared deploy script (`b5fb2d3`) — all mirroring the proven `dmt-dnit/petgiftshop` deploy pattern, fetched live via `gh api` rather than reconstructed from memory. Coordinator caught and fixed one pin-currency issue before integration (V-3's first draft copied Pet Giftshop's `checkout@v4`/`setup-java@v4` verbatim instead of this repo's already-current `@v7`/`@v5`). **Codex round-1 REJECTED** (`reviews/sprint-22-track-c-review.md`) on two genuine, coordinator-verified findings: **P1** — all three deploy workflows trusted `ssh-keyscan` output at runtime with no host-key pinning (MITM risk on the SSH deploy path), inherited verbatim from the Pet Giftshop reference, not implementer-introduced. **P2** — WebSocket upgrade headers were applied to the catch-all `location /` instead of a dedicated `/ws` block in the order/inventory vhosts — ***coordinator brief error***: the V-2 brief explicitly told the implementer to put the headers in `location /`, mirroring the Pet Giftshop template's single-block shape without accounting for this project's WebSocket traffic living at one specific path. **Round-2 fix** (`341554b`, opencode+DeepSeek): real `dnit-vps` host key (RSA/ECDSA/Ed25519) captured from an already-trusted SSH session and committed to `deploy/ssh/dnit-vps.known_hosts` (host keys are public by design — pinning the value in-repo is correct practice, unlike the SSH private key which stays a GitHub secret); all three workflows now `cat` that file instead of scanning at runtime. Nginx configs split into a dedicated `location /ws` (upgrade headers) + plain `location /` (no upgrade headers) for orders/inventory; `saga-auth.conf` correctly untouched. `pre-review-check.sh 22` passes both rounds. **This sprint produced deploy *artifacts* only — no live infrastructure was touched** (no SSH commands against `dnit-vps` beyond the coordinator's own read-only port-verification check earlier in Track C scoping, no systemd/Nginx installed live, no GitHub secrets/Environment created, no workflow dispatched). The live-apply session is separately scheduled with Dimitri in the loop, per `docs/backlog/track-c-go-live-roadmap.md`'s artifact-authoring/live-apply split.
**Live-apply session — DONE 2026-07-15, same day (`docs/backlog/sprint-22-live-apply-runbook.md`).** All three services now live on `dnit-vps`: `https://saga-{auth,orders,inventory}.dnit.be` all return `200` on health/JWKS, H2 console confirmed blocked (`401`/`404`, never `200`). Dimitri ran Phases 1–2 (user/systemd/Nginx/certbot) himself first to learn the process, granted the coordinator temporary full NOPASSWD sudo partway through (revoked immediately after, replaced by a narrow exact-command allowlist for Phase 3's CI credentials). **Two real bugs found only by actually running this — neither catchable by Codex's artifact review, which explicitly didn't run a live deploy:** (1) the sudoers rule's `systemctl status` entries were missing `--no-pager`, which `deploy-backend.sh` calls exactly — sudoers matches exact arguments, so this would have failed the deploy script's last step despite everything before it succeeding; caught by testing the literal command before Phase 4, not assumed from the rule reading correctly. (2) **The bigger one:** all three deploy workflows shared identical remote temp paths (`/tmp/backend.jar`, `/tmp/deploy-backend.sh`), copied verbatim from the single-app Pet Giftshop template. Dispatching all three concurrently caused a real race: 2 of 3 deploys failed outright, and the "successful" one — `pubrec-auth` — actually started **inventory-service's jar** under the auth-server's systemd unit. Caught only because the coordinator checked the running process's own structured log for self-identification (`"service":{"name":"inventory-service"}`) instead of trusting `systemctl status`'s "active (running)" — stopped immediately, no other service on the box affected. Fixed with **round 3** (`fix(sprint-22-round3)`, `c1dc0b3`, opencode+DeepSeek): unique per-service temp paths; redeployed sequentially afterward, each verified by log self-identification, not just uptime. **New gap surfaced, not yet a scoped task:** there is no Kafka broker on `dnit-vps` at all — `order-service`/`inventory-service` are healthy but their Kafka listeners retry `localhost:9092` forever, so the actual saga (the point of the demo) doesn't work yet. Missed when Sprint 22 was scoped; the roadmap's "Kafka: always-on" resource-budget decision never became deploy artifacts. Tracked as Phase 3.5 in the roadmap, blocking Phase 4 (Vercel) in practice since there's nothing to demo without a working saga.
**Sprint 21 (dependency-currency triage) — CLEARED 2026-07-14 (human-confirmed ACCEPT).** Cleared the routine slice of 22 Dependabot PRs that piled up since Sprint 16: **D-1** CI Actions `checkout`/`upload-artifact` → v7 (`b03f211`), **D-2** `maven-enforcer-plugin` 3.6.3 + `testcontainers:kafka`/`junit-jupiter` 1.21.4 in the two services, core `testcontainers` left BOM-managed (`67635c2`), **D-3** Angular family bumped to actual npm-registry latest (`^22.0.6` core/forms/animations/etc., `^22.0.4` material/cdk) — the implementer correctly caught and fixed a real `ERESOLVE` peer-conflict by moving the whole Angular family in lockstep rather than forcing around it (`8563b88`). All three coordinator-verified against each task's own branch base (not stale-relative-to-`main` diffs) per `[[feedback-claude-worktree-stale-base]]`. Explicitly out of scope and still open: Testcontainers core major bump 1.21.3→2.0.5 (BOM-managed, needs its own Boot-4.1 compat task, Dependabot PRs #10/#14 left open on purpose) and 3 stale `spring-boot-starter-parent` PRs (#2/#11/#12, already satisfied on `main` since Sprint 16 — closed manually as no-ops). **Review process note:** Codex's review (`reviews/sprint-21-track-b-review.md`) was substantively a clean accept — verified every task against the handoff with file:line citations, independently re-ran `npm audit --omit=dev` (0 vulnerabilities, matching the handoff), stated its own residual verification gaps honestly — but its `Verdict:` line read `**No blocking findings**`, which doesn't contain the literal `ACCEPT`/`REJECT` token the machine-header contract (`docs/backlog/review-machine-header.md`) requires. `scripts/verify-review.sh 21` correctly returned **exit 4 UNKNOWN** rather than guess. Per the "fix the metadata, don't guess" rule, the coordinator did not rewrite Codex's verdict line — Dimitri reviewed the content directly (Codex also restated the same findings in chat, matching the file) and gave a **human override to ACCEPT**, logged here rather than silently treated as if the script had passed. Also decided this sprint: the **merge-gate policy** (`CLAUDE.md`, "Merge gate: direct-to-main vs. review-branch") — Dimitri flagged that pushing straight to `main` before Codex review doesn't match normal PR-gated practice; after discussion the policy adopted is scoped to the *supervision* boundary, not mechanical-vs-logic-bearing: coordinator-supervised work (this sprint included) keeps pushing direct to `main`; only the AUTO-3 operational driver's future unattended pushes go through a review branch held until Codex ACCEPTs.
**Sprint 23 (Track C Phase 3.5, Kafka deploy artifacts) — CLEARED 2026-07-15.** Codex **genuine ACCEPT, round 1** (`reviews/sprint-23-track-c-review.md`, target `1728fcd`, machine header compliant). Delivered `deploy/docker-compose/kafka-vps.yml` + `deploy/systemd/pubrec-kafka.service` + a new "Phase 3.5" runbook section (`1728fcd`) closing the gap Sprint 22's live-apply surfaced: no Kafka broker existed on `dnit-vps`, so the saga couldn't work even though both dependent services were healthy. Adapted directly from `order-service/docker-compose.yml` (coordinator diffed the two files directly to confirm the *only* changes were `127.0.0.1`-only port binding — Kafka's `PLAINTEXT` listener has no auth, so binding to Docker's `0.0.0.0` default would expose the broker publicly — plus `restart: unless-stopped` and a `name: pubrec` namespace; every image version and Kafka/ZooKeeper env var stayed byte-identical to the already-tested local config). Codex independently confirmed the loopback binding and cross-checked it against the actual client config in both services' `application.yml`. Artifacts only, same split as Sprint 22 — live-apply (installing the compose stack + systemd unit on the box, restarting `order`/`inventory` to pick up the broker) not yet scheduled.  
**Sprint 24 (OutboxRelay self-invocation fix) — CLEARED 2026-07-15.** Codex **genuine ACCEPT, round 1** (`reviews/sprint-24-track-b-review.md`, target `9cf3f7f`, machine header compliant; independently re-ran the 3 targeted tests itself). Real application-logic bug, not deploy config — found live during Track C Phase 3.5's Kafka deploy: once Kafka became reachable, `inventory-service`'s `OutboxRelay.scheduledRelay()` failed on every ~500ms firing with `InvalidDataAccessApiUsageException: No active transaction`. **Root cause:** classic Spring AOP self-invocation — `scheduledRelay()` called `processPending()` via the implicit `this.` reference, bypassing the proxy that makes `@Transactional` (on `processPending()`) actually take effect. Existing tests (`OutboxIntegrationTest`, `OutboxRelayConcurrencyTest`) never caught this because they call `processPending()` on an `@Autowired`-injected bean reference, which *does* go through the proxy. **Practical impact: the saga's return leg was broken** — `inventory-service` could consume `OrderPlacedEvent` but the relay publishing `InventoryReservationEvent` back to Kafka never once succeeded, since this class was written. **Fix** (`9cf3f7f`, opencode+DeepSeek): `ObjectProvider<OutboxRelay>` self-injection so `scheduledRelay()` routes through `self.getObject().processPending()`; `processPending()` itself byte-for-byte unchanged (existing tests depend on its exact signature). New regression test `OutboxRelayScheduledInvocationTest` calls `scheduledRelay()` — not `processPending()` — through the proxy; implementer confirmed it fails against the pre-fix code with the exact live error before reapplying the fix. Coordinator independently rebuilt from integrated `main` and re-ran the 3 relevant test classes: BUILD SUCCESS, 0 failures, 0 errors. **Live-verified 2026-07-15 same day** — redeployed `inventory-service` via `deploy-inventory.yml`, then placed a **real order through the live public API** (`POST https://saga-orders.dnit.be/api/orders`, seeded `customer1@example.test` JWT) as the actual proof, not just absence-of-error: inventory-service's log shows the exact order ID relayed successfully (`"Relayed outbox event id=1 orderId=0298accb... status=RESERVED"`, no more transaction error), and `GET /api/orders` confirms the order's status closed the full loop, `PENDING` → `CONFIRMED`. **This is the first genuine end-to-end saga proof on live infrastructure** — order placed → Kafka → inventory reserved → Kafka → order confirmed, all on the public `saga-*.dnit.be` endpoints.  
**Sprint 19 (Track B Sprint 6) — CLEARED 2026-07-03.** Codex round-2 **genuine ACCEPT** (`reviews/sprint-19-track-b-round-2-review.md`, target `9b8216e`: B-8 P1+P2 fixed, B-9 no source defect + live CI green) after Dimitri re-sent a fresh round-2 request (bypassing the dedup bug below). History: **live CI all green incl. the new `e2e-smoke` job** (B-9 `e305061` — the automated B-6 runtime proof: stack healthy → Playwright smoke incl. F2 `toHaveCount(+1)`; Codex found no source defect there). **Codex round-1 REJECTED on B-8** (`8d2bbd8`, `reviews/sprint-19-track-b-review.md`): **P1** — the auth-server JWKS poll in `startup-all.sh` is a non-failing loop (exhausts 30 tries then continues; no `exit 1` like the new `wait_for`), so downstream services can start against a dead auth server — *coordinator brief error: it told the implementer to keep that loop "as-is."* **P2** — the no-compose fallback selects `docker` whenever the CLI exists even if only podman's daemon works. **Round-2 fix applied** (`9b8216e`, opencode+DeepSeek single repo-root run): auth JWKS → fail-fast `wait_for`; engine selection probes `podman info`→`docker info`→`exit 1` (prefers podman). Fallback now fully consistent (every gate fail-fast, no `sleep`-gates). `pre-review-check.sh 19` passes. **`9b8216e`+`7444681` now pushed to origin/main** (Dimitri fixed WSL `gh` auth; coordinator pushed). **Codex round-2 run (2026-07-01) was a STALE FALSE-REJECT** — its "resumed" review re-stated the round-1 P1/P2 findings verbatim (old line refs 30–31, 83–87) and wrote NO new file (`reviews/sprint-19-track-b-review.md` + demo-notes untouched since 17:26 round-1, 0 mentions of `9b8216e`). Coordinator re-read `startup-all.sh` at HEAD and **verified both fixes are genuinely present**: JWKS wait is fail-fast `wait_for` (L89→`exit 1`); engine picks `podman info`→`docker info`→`exit 1` (L30–37, working-daemon probe, prefers podman). **Root cause = automation dedup bug, not code:** the Codex app job treats a sprint as "already handled if the thread contains a review request for the same sprint handoff," so an *updated* `sprint-19-handoff.md` (same basename/number) never re-triggered a fresh round-2 request. Fix the automation to key on sprint+mtime/round-marker, not sprint number alone — briefed as **AUTO-1** (Dimitri's, Codex-app prompt edit; covers both the review dispatcher and the demo-notes dispatcher, which share the bug). The stale-reject discovery was manual this round; it is now caught mechanically by **AUTO-2** `scripts/verify-review.sh` (landed `eb9244c`, wired into the cadence as step 4.5). Both tasks = opencode+DeepSeek; handoff `docs/backlog/sprint-19-handoff.md` (now carries `Review-Target-Commit: 9b8216e`). First dispatch tangled (pre-made worktrees + concurrent runs) — recovered, no repo damage; verified recipe = single run from repo root ([[feedback-opencode-worktree-sandbox]]).
**Next sprint entry point:** Sprints 28–31 all resolved (28 rejected→fixed by 30, 29/30/31 all genuine ACCEPT). **Two threads open, running outside the normal sprint cadence:** (1) a real, unresolved bug — auth JWT token vanishes from `order-ui`'s `localStorage` on a plain page refresh, extensively ruled-out list in `docs/backlog/investigation-localstorage-token-loss.md`, handed to a separate browser-capable Codex agent for live debugging (deliberately kept isolated from the review loop, Dimitri's call 2026-07-17). (2) A real, found-but-not-yet-fixed issue: `auth-server`'s JWT signing `KeyPair` (`JwtConfig.java`) regenerates fresh and randomly on every restart, invalidating all sessions on every future redeploy — natural next fix, mirrors Sprint 29's pattern (persist the key to a file). Two small open decisions from Sprint 28, neither blocking: whether to add the admin approval page to `inventory-ui` too (Dimitri leaned yes, not committed — see Sprint 28 entry above), and fine-grained/multi-role permissions (explicitly deferred, revisit only if this grows toward a prod-grade project). Track C, `docs/backlog/track-c-go-live-roadmap.md`. **Phase 4 is fully done** (Sprint 26 code + Sprint 27 OAuth-redirect fix, both Codex-ACCEPTed, both frontends live on Vercel, backend CORS redeployed and live-verified — see Sprint 27 entry above). Next open item is **Phase 5** (Snyk CI gate promotion, independent, can slot in anytime) or **Phase 6** (real Google OAuth end-to-end — gated on the Phase 2 secrets-policy decision, and note Sprint 27 only fixed the redirect *entrypoint*; `auth-server`'s OAuth2 success handler still hardcodes `http://localhost:4200/login?oauth2=success` and no real Google credentials exist anywhere in this repo, both still Phase 6 work). Workflow-automation loop (AUTO-1/2/3): AUTO-1 (Codex-app dispatcher round-aware fix, Dimitri's prompt edit) — **Dimitri reports done** 2026-07-15; not independently verifiable by the coordinator (no repo diff for a Codex-app prompt change, same limitation noted when this was briefed). AUTO-2 (`scripts/verify-review.sh`) and AUTO-3 (`scripts/review-intake.sh`) landed earlier, both coordinator-verified against real reviews. The **operational driver** (interval runner for `review-intake.sh` + push + notify, Track C Phase 7) is still not built — needs Dimitri's sign-off, unchanged. Phase 3.5 (Kafka broker on `dnit-vps`) is fully done (Sprint 23 + live-apply, see above) — this line previously called it "new, unscoped," which was stale; corrected here. **Candidate backlog / deferred (unchanged):** real Google OAuth end-to-end + CI (needs the Phase 2 secrets-policy decision first); promote the Snyk CI gate from report-only (`continue-on-error`) to blocking once the dev-only Angular CVEs clear; Testcontainers core major bump (1.21.3→2.0.5, deferred at Sprint 21 scoping). 
<!-- prior Sprint 16 scope detail (now done): SB-3 (Boot-4 cleanup — drop classic bridge + `use-jackson2-defaults` + Jackson-2 jsr310, real Jackson 3, → 4.1.0; highest-risk, touches saga serialization), **B-4** (OpenAPI/Swagger via springdoc 3.0.x), **SEC-1** (server-side CVE scan in CI + Dependabot). Overview `docs/backlog/sprint-16.md`; briefs in `docs/backlog/tasks/sprint-16/`. Run SB-3 first (restructures poms). Implementers TBD — **awaiting dispatch go-ahead.** Deferred to Sprint 17+: inventory exactly-once/outbox, B-2 (observability), B-3 (Testcontainers), B-6 (Docker Compose). Fuller backlog detail: **(0) Boot-4 modernization cleanup** — remove the `spring-boot-starter-classic` bridge + adopt modular starters + migrate off `use-jackson2-defaults` to real Jackson 3 (Sprint 15 left these compatibility shims, accepted by Dimitri); plus inventory exactly-once/outbox hardening + server-side vuln scanning (Sprint 14 follow-ups 2 & 3); plus Track B B-2 (observability), B-3 (Testcontainers), B-4 (OpenAPI), B-6 (Docker Compose). B-7 done. -->  
**Pre-review command:** `bash scripts/pre-review-check.sh <sprint-number>`

**Active caveats (do not re-litigate each sprint — update only when signal changes):**
- `npm audit --omit=dev` → 0 vulnerabilities in both UIs (production clean)
- Full `npm audit` → 8 dev-tooling advisories per UI (3 low/3 mod/2 high) under `@angular-devkit/build-angular`'s build/dev-server chain — incl. **http-proxy-middleware High** (CRLF injection in `fixRequestBody`, dev-server proxy only), webpack-dev-server, @babel/core, uuid. **Dev-only — not in the shipped bundle; production audit = 0.** No forward fix: Angular **22.0.6 is the latest on the registry** (bumped Sprint 21, was 22.0.4) and is itself in the vulnerable range; the only npm auto-fix is a downgrade to Angular 21 (rejected). Recheck when Angular ships a patched 22.x. Surfaced via Dependabot (SEC-1, alerts enabled 2026-06-29) — dev-only alerts there are expected, not blockers. **Sprint 21 update:** count still 8, but composition shifted — a new esbuild Windows-arbitrary-file-read advisory (dev server only) replaced one of the prior set; still entirely inside the same dev/build chain
- Angular webpack builder deprecation warning — not a blocker yet; track at next Angular version upgrade
- Mockito/Java agent self-attach warning (OpenJ9 environments) — accepted, documented in Sprint 2 review
- Testcontainers/Surefire shutdown warning in inventory-service — exit 0, noisy; clean up in B-3
- ~~GitHub Actions Node 20 deprecation~~ — RESOLVED Sprint 15: actions bumped to checkout@v5, setup-java@v5, setup-node@v6 (`f190f68`); confirm on next live CI run
- ~~Spring Boot 4.0.7 compatibility shims~~ — RESOLVED Sprint 16 (SB-3): on Boot **4.1.0**, classic bridge + `use-jackson2-defaults` removed, Kafka serde on Jackson 3 (`JacksonJson*`). Spring Security 7.1.0 (patched). springdoc 3.0.3, OWASP CI scan + Dependabot added (SEC-1).

**Track A history (for reference):** 13 sprints, Sprints 1–13, closed 2026-06-28. Full Playwright smoke test (login → order placement → Kafka saga → live status update → inventory decrement) passes against a non-empty local DB. See `docs/backlog/track-a-retro.md` for lessons learned.

All 4 Maven modules have working `mvnw.cmd` (Windows) and `mvnw` (Unix) wrappers. Maven Enforcer requires Java 21 (`[21,22)`) and fails fast otherwise.

## Architecture

### Auth flow
`auth-server` is the single source of truth for identity. It signs JWTs with an RSA key pair and publishes the public key via JWKS (`/oauth2/jwks`). `order-service` and `inventory-service` are pure JWT **resource servers** — they have no user store of their own; they validate tokens by fetching `auth-server`'s JWKS at `http://localhost:9000/oauth2/jwks` (see each service's `config/SecurityConfig.java`).

Registration is gated: new users land in `UserEntity.Status.PENDING` and cannot log in until an admin flips them to `ACTIVE` via `AdminController` (`PUT /api/admin/users/{id}/approve`, requires `ROLE_ADMIN`). Roles are carried in the JWT's `role` claim and mapped to Spring `ROLE_*` authorities by `JwtAuthenticationConverter` in each service's security config. Seeded roles: `ADMIN`, `CUSTOMER`, `WAREHOUSE_STAFF`.

`auth-server` uses an in-memory H2 database (`DataSeeder` seeds initial data on startup) and also supports Google OAuth2 login as an alternate path into the same JWT issuance flow.

### Kafka choreographed saga
`order-service` and `inventory-service` implement a two-step choreographed saga over Kafka:

- `order-service` owns `Order` locally, exposes `POST /api/orders`, publishes `OrderPlacedEvent` to `order-events`, consumes from `inventory-events`.
- `inventory-service` owns `Product`/`Stock` locally, consumes from `order-events`, attempts to reserve stock, publishes `InventoryReservationEvent` (`RESERVED` or `REJECTED`) to `inventory-events`.
- `order-service` consumes `inventory-events` and updates order status, closing the saga loop.

Each service's listener pushes received events to its own connected browsers over STOMP/WebSocket (`SimpMessagingTemplate` → `/topic/messages`), which the corresponding Angular UI subscribes to via `websocket.service.ts`.

The shared event contracts live in `shared-model`: `OrderPlacedEvent`, `InventoryReservationEvent`, `OrderItem`. Both services trust this package for JSON deserialization (`spring.json.trusted.packages` in each `application.yml`).

A local Kafka broker is required to run the publish/consume flow; `order-service/docker-compose.yml` provides a single-node Kafka+Zookeeper stack on the default ports (`2181`, `9092`).

### shared-model contract boundary
`shared-model` holds only things that cross a service boundary on the wire: DTOs (`LoginRequest`, `LoginResponse`, `RegisterRequest`) and Kafka events (`OrderPlacedEvent`, `InventoryReservationEvent`, `OrderItem`). Each service owns its JPA-persisted entities locally (`order-service` owns `Order`, `OrderLineItem`; `inventory-service` owns `Product`; `auth-server` owns `UserEntity`).

### Frontend
`order-ui` and `inventory-ui` are structurally identical Angular 22 + Angular Material SPAs (compare `proxy.conf.json` in each to see which backend port they target). Both proxy `/api/auth/**`, `/oauth2/**`, `/login/**` to `auth-server` (9000). `order-ui` proxies `/api/orders/**` + `/ws/**` to `order-service` (8080); `inventory-ui` proxies `/api/inventory/**` + `/ws/**` to `inventory-service` (8081). `auth.interceptor.ts` attaches the JWT as a Bearer token on outgoing requests; `auth.guard.ts` gates routes on `AuthService.isLoggedIn()`.

## Common commands

### Java services (Maven)
Build order matters — `shared-model` must be installed to the local Maven repo before the services that depend on it will compile:

```bash
cd shared-model && ./mvnw clean install
cd ../auth-server && ./mvnw clean compile
cd ../order-service && ./mvnw clean compile
cd ../inventory-service && ./mvnw clean compile
```

The Maven Enforcer plugin is configured in each module to require Java 21 (`<requireJavaVersion>[21,22)</requireJavaVersion>`). Builds fail fast if you're not on JDK 21.

Run a single service: `./mvnw spring-boot:run` from within that service's directory.

Run tests for one module: `./mvnw test` (from `order-service` or `inventory-service`; e.g. `./mvnw -Dtest=OrderEventPublisherTest test` for a single test class).

Start the local Kafka broker needed by `order-service`/`inventory-service`: `cd order-service && docker compose up -d`.

### Angular UIs
Each UI is a separate npm project — there's no root `package.json`:

```bash
cd order-ui && npm install && npm start     # ng serve, port 4200
cd inventory-ui && npm install && npm start # ng serve, port 4201
```

`npm start` runs `ng serve` with `proxyConfig: proxy.conf.json`, so the backing Java services must already be running for API/WebSocket calls to succeed.

### Full local stack
To exercise the whole demo end-to-end: start the Kafka broker (`order-service/docker-compose.yml`), then `auth-server` (9000), then both `order-service` (8080) and `inventory-service` (8081), then `order-ui` (4200) and/or `inventory-ui` (4201).
